name: Cleanup Old Images

on:
  schedule:
    - cron: '0 2 * * 0'  # Weekly on Sunday at 2 AM
  workflow_dispatch:  # Manual trigger

jobs:
  cleanup:
    name: Remove Old Docker Images
    runs-on: ubuntu-latest
    
    steps:
      - name: Delete old images
        run: |
          echo "Manual cleanup of old Docker images"
          echo "Visit Docker Hub to manage image retention policies"
          echo "Backend: ${{ secrets.DOCKER_USERNAME }}/todo-backend"
          echo "Frontend: ${{ secrets.DOCKER_USERNAME }}/todo-frontend"
